import React from 'react';
import classNames from './styles.scss';

const Footer = () => {
  return (
    <footer className={classNames.footer}>All Right reserver</footer>
  );
};

export default Footer;
